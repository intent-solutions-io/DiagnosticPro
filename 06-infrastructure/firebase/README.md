# Firebase Configuration

Configuration for Firebase Hosting and services.

## Files

- `firebase.json` - Firebase project configuration
- `.firebaserc` - Firebase project aliases
- `hosting/` - Hosting-specific configuration
