# Vertex AI Configuration (Production)
VAI_MODEL=gemini-2.0-flash-exp
VAI_LOCATION=us-central1
GCP_PROJECT=diagnostic-pro-prod

# Note: Using experimental model due to 404 errors on stable models (gemini-1.5-flash*)
# Stable models may require additional API enablement or project permissions
# GOOGLE_APPLICATION_CREDENTIALS uses Application Default Credentials
# Service account: diagnosticpro-backend@diagnostic-pro-prod.iam.gserviceaccount.com
# Authentication: Workload Identity (no exported keys)
